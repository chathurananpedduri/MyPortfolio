#!/bin/sh

# Maven wrapper script

MAVEN_VERSION=3.8.1
MVN_CMD="mvn"

if [ -n "$MAVEN_OPTS" ]; then
  MVN_CMD="$MVN_CMD -DskipTests"
fi

if [ -n "$1" ]; then
  exec "$MVN_CMD" "$@"
else
  exec "$MVN_CMD" clean install
fi